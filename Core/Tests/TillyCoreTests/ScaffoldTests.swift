import Testing
@testable import TillyCore

/// Proves the test harness runs. Delete once real engine tests exist.
@Test func recurrenceUnitHasFourCases() {
    #expect(RecurrenceUnit.allCases.count == 4)
}
